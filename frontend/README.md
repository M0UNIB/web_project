# Frontend

This Vite + React 18 client talks to the Laravel API through `/api`.

## Commands

```bash
npm install
npm run dev
```

The development server uses a Vite proxy to forward `/api` requests to `http://127.0.0.1:8000`.
